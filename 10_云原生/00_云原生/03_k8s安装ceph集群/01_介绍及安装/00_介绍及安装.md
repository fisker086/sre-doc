# Rook-Ceph介绍及安装

## 一、Rook介绍

>官网地址  https://rook.io
>
>项目地址  https://github.com/rook

### 1、Ceph介绍

>Ceph是一种高度可扩展的分布式存储解决方案，提供对象、文件和块存储。在每个存储节点上，您将找到Ceph存储对象的文件系统和Ceph OSD(对象存储守护程序)进程。在Ceph集群上，您还可以找到Ceph MON(监控)守护程序，它们确保Ceph集群保持高可用性。

### 2、Rook

>Rook 是一个开源的cloud-native storage编排, 提供平台和框架；为各种存储解决方案提供平台、框架和支持，以便与云原生环境本地集成。
>
>Rook 将存储软件转变为自我管理、自我扩展和自我修复的存储服务，它通过自动化部署、引导、配置、置备、扩展、升级、迁移、灾难恢复、监控和资源管理来实现此目的。
>
>Rook 使用底层云本机容器管理、调度和编排平台提供的工具来实现它自身的功能。
>
>Rook 利用扩展功能将其深度地集成到云原生环境中，并为调度、生命周期管理、资源管理、安全性、监控等提供了无缝的体验。
>
>Rook 目前支持Ceph、NFS、Minio Object Store和CockroachDB。
>
>Rook 支持的最稳定的存储仍然是 Ceph，
>
>Rook 在 2018 年发布的 0.9 版本中，正式将 Ceph Operator 作为稳定支持的特性，迄今已经数年。
>
>使用 Rook 部署和管理生产级别的 Ceph 集群还是非常稳健的。

## 二、Rook架构

![image-20250818191516457](https://picture-bed.gmbaifa.online/PicGo/image-20250818191516457.png)

### 1、Rook、Ceph、Rook的关系

>Rook 的本意是为了降低部署管理 Ceph 集群的难度，
>Ceph 是一个高度可扩展的分布式存储解决方案，用于块存储、对象存储和共享文件系统。
>
>Rook部署以及管理都在k8s中运行管理，
>Rook用于编排ceph集群部署工具主要使用operator管理操作。
>而Rook存储运行则完全自动化。Rook存储是通过第三方资源以kubernetes扩展形式运行。
>
>Rook operator 是一个简单的容器，它拥有引导和监控存储集群所需的一切。
>操作员将启动和监控Ceph，监控 pod，提供 RADOS 存储的 Ceph OSD 守护进程，以及启动和管理其他 Ceph 守护进程。
>
>操作员通过初始化运行服务所需的 Pod 和其他资源来管理池、对象存储 (S3/Swift) 和文件系统的 CRD。

### 2、Rook包含的组件

#### 1.Rook Operator

>Rook Operator是rook的大脑，以deployment形式存在，其利用k8s的controller-runtime框架实现了CRD，并进而接受k8s创建资源的请求,并创建相关资源(集群，pool，块存储服务，文件存储服务等)。
>
>Rook Operator监控存储守护进程，来确保存储集群的健康。
>监听Rook Discovers收集到的存储磁盘设备，并创建相应服务(ceph的话就是osd了)。
>注：可以通过修改operate.yaml中的replicas的副本数来保证Operate的高可用(默认为1)。

#### 2.Rook Discover

>Rook Discover是以daemonset形式部署在所有的存储机上的，其检测挂接到存储节点上的存储设备。
>
>把符合要求的存储设备记录下来，这样Rook Operator感知到以后就可以基于该存储设备创建相应服务了。

#### 3.Rook Agent

>Rook Agent是以daemonset形式部署在所有的存储机上的，其处理所有的存储操作，例如挂卸载存储卷以及格式化文件系统等。

## 三、Ceph Rook部署

### 1、集群准备

>k8s集群准备
>
>helm准备
>
>空闲硬盘准备,必须清除GPT或者mbr分区
>
>```bash
>sgdisk --zap-all /dev/sdd
>```
>
>磁盘节点lvm工具准备
>
>```bash
>apt-get install -y lvm2
>```

### 2、安装ceph-rook

>官方文档：https://rook.github.io/docs/rook/v1.17/Getting-Started/Prerequisites/prerequisites/

>作为存储节点，需要存储节点有相应的raw设备，即空闲未使用的硬盘，如每个节点增加sdb盘,修改cluster.yaml（rook-1.xx.x/deploy/examples目录下）文件，指定mon、mgr节点个数,以及资源使用限制；另外，需指定osd节点信息，并初始化相应磁盘。

#### 1.下载Rook

```bash
wget https://github.com/rook/rook/archive/refs/tags/v1.17.7.tar.gz

tar -zxvf v1.17.7.tar.gz
```

#### 2.修改配置文件

```bash
cd rook-1.17.7/deploy/examples/
vim cluster.yaml
...
   provider: host # 使用宿主机网络
...
  placement:
   all:
     nodeAffinity:
       requiredDuringSchedulingIgnoredDuringExecution:
         nodeSelectorTerms:
         - matchExpressions:
           - key: kubernetes.io/hostname
             operator: In
             values:
             - k8s-node-01
             - k8s-node-02
             - k8s-node-03
...
  storage: # cluster level storage configuration and selection
    useAllNodes: false # 不在所有节点尝试部署 OSD（前提是节点上有可用存储设备）
    useAllDevices: false # 不使用所有未被使用的块设备（raw disk）来创建 OSD。
    nodes:
      - name: "k8s-node-01"
        devices:
          - name: "/dev/disk/by-id/ata-QEMU_HARDDISK_QM00005"
            config:
              metadataDevice: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi1"
      - name: "k8s-node-02"
        devices:
          - name: "/dev/disk/by-id/ata-QEMU_HARDDISK_QM00005"
            config:
              metadataDevice: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi1"
      - name: "k8s-node-03"
        devices:
          - name: "/dev/disk/by-id/ata-QEMU_HARDDISK_QM00005"
            config:
              metadataDevice: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi1"
...
```

#### 3.部署

```bash
kubectl create -f crds.yaml -f common.yaml
kubectl apply -f operator.yaml
kubectl apply -f cluster.yaml
kubectl apply -f toolbox.yaml
```

#### 4.进入rook-ceph-tools的pod检查ceph状态

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl exec -it -n rook-ceph rook-ceph-tools-7b75b967db-2sg8z -- /bin/sh
sh-5.1$ ceph -s
  cluster:
    id:     c87a87ff-f4a5-462b-9fd5-87ca234bb876
    health: HEALTH_OK
 
  services:
    mon: 3 daemons, quorum a,d,b (age 90m)
    mgr: a(active, since 93m), standbys: b
    osd: 3 osds: 3 up (since 3m), 3 in (since 4m)
 
  data:
    pools:   1 pools, 1 pgs
    objects: 2 objects, 577 KiB
    usage:   600 GiB used, 16 TiB / 17 TiB avail
    pgs:     1 active+clean
```

#### 5.查看创建的目录树

```bash
root@k8s-node-01:~# cd /var/lib/rook/
root@k8s-node-01:/var/lib/rook# tree 
.
|-- exporter
|   |-- ceph-client.ceph-exporter.asok
|   |-- ceph-mgr.a.asok
|   |-- ceph-mon.a.asok
|   `-- ceph-osd.0.asok
|-- mon-a
|   `-- data
|       |-- external_log_to
|       |-- keyring
|       |-- kv_backend
|       |-- min_mon_release
|       `-- store.db
|           |-- 000247.log
|           |-- 000249.sst
|           |-- CURRENT
|           |-- IDENTITY
|           |-- LOCK
|           |-- MANIFEST-000010
|           |-- OPTIONS-000007
|           `-- OPTIONS-000012
|-- rook-ceph
|   |-- c87a87ff-f4a5-462b-9fd5-87ca234bb876_ab4d8041-a59e-4d55-a8f9-f9de6d8a4942
|   |   |-- block -> /dev/ceph-afc3294a-e5e8-49c1-a30f-0968417f6dfd/osd-block-ab4d8041-a59e-4d55-a8f9-f9de6d8a4942
|   |   |-- block.db -> /dev/ceph-75218d74-77f6-466d-8aa0-023568963438/osd-db-f319ef42-fb31-4bf4-b511-7d3d7b6f2981
|   |   |-- ceph_fsid
|   |   |-- fsid
|   |   |-- keyring
|   |   |-- ready
|   |   |-- require_osd_release
|   |   |-- type
|   |   `-- whoami
|   |-- client.admin.keyring
|   |-- crash
|   |   `-- posted
|   |-- log
|   |   |-- ceph-client.ceph-exporter.log
|   |   |-- ceph-mgr.a.log
|   |   |-- ceph-mon.a.log
|   |   |-- ceph-osd.0.log
|   |   `-- ceph-volume.log
|   `-- rook-ceph.config
|-- rook-ceph.cephfs.csi.ceph.com
|   `-- log
|       |-- controller-plugin
|       `-- node-plugin
`-- rook-ceph.rbd.csi.ceph.com
    `-- log
        `-- node-plugin

17 directories, 32 files
root@k8s-node-01:/var/lib/rook# 
```

#### 6.查看lvm创建

```bash
root@k8s-node-01:/var/lib/rook# pvscan
  PV /dev/sdd   VG ceph-afc3294a-e5e8-49c1-a30f-0968417f6dfd   lvm2 [<5.46 TiB / 0    free]
  PV /dev/sdb   VG ceph-75218d74-77f6-466d-8aa0-023568963438   lvm2 [<200.00 GiB / 0    free]
  Total: 2 [5.65 TiB] / in use: 2 [5.65 TiB] / in no VG: 0 [0   ]
```

### 3、dashboard准备

```bash
kubectl apply -f dashboard-external-https.yaml

# 查看密码
kubectl -n rook-ceph get secret rook-ceph-dashboard-password -o jsonpath="{['data']['password']}" | base64 --decode && echo
```

## 四、使用

### 1、RBD-StorageClass准备

```bash
cd deploy/examples/csi/rbd

kubectl apply -f storageclass.yaml

root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples/csi/rbd# kubectl apply -f storageclass.yaml 
cephblockpool.ceph.rook.io/replicapool created
storageclass.storage.k8s.io/rook-ceph-block created
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples/csi/rbd# kubectl get sc
NAME              PROVISIONER                  RECLAIMPOLICY   VOLUMEBINDINGMODE   ALLOWVOLUMEEXPANSION   AGE
rook-ceph-block   rook-ceph.rbd.csi.ceph.com   Delete          Immediate
```

### 2、CEPHFS-StorageClass准备

#### 1.安装cephfs元数据存储池及插件storageclass

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f filesystem.yaml 
cephfilesystem.ceph.rook.io/myfs created
cephfilesystemsubvolumegroup.ceph.rook.io/myfs-csi created

```

#### 2.以pod的形式部署在rook-ceph命名空间中，会有两个pod。

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl -n rook-ceph get pod -l app=rook-ceph-mds
NAME                                    READY   STATUS    RESTARTS   AGE
rook-ceph-mds-myfs-a-5bc9df7587-8cwrg   2/2     Running   0          75s
rook-ceph-mds-myfs-b-5b6fb58c75-mt8nj   2/2     Running   0          74s
```

#### 3.查看创建结果

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl get sc
NAME              PROVISIONER                     RECLAIMPOLICY   VOLUMEBINDINGMODE   ALLOWVOLUMEEXPANSION   AGE
rook-ceph-block   rook-ceph.rbd.csi.ceph.com      Delete          Immediate           true                   12m
rook-cephfs       rook-ceph.cephfs.csi.ceph.com   Delete          Immediate           true                   4s
```

#### 4.知识点

```bash
cephfs使用和rbd一样指定storageClassName的值即可，需要注意的是rbd只支持ReadWriteOnce，cephfs可以支持ReadWriteMany。

pv的三种访问模式
ReadWriteOnce，RWO，仅可被单个节点读写挂载
ReadOnlyMany，ROX，可被多节点同时只读挂载
ReadWriteMany，RWX，可被多节点同时读写挂载
 
pv回收策略
Retain，保持不动，由管理员手动回收
Recycle，空间回收，删除所有文件，仅NFS和hostPath支持
Delete，删除存储卷，仅部分云端存储支持
```

### 3、开启S3 API接口

#### 1.创建对象存储

>Rook操作员将创建启动服务所需的所有池和其他资源。

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f object.yaml 
cephobjectstore.ceph.rook.io/my-store created
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl -n rook-ceph get pod -l app=rook-ceph-rgw
NAME                                       READY   STATUS    RESTARTS   AGE
rook-ceph-rgw-my-store-a-96c7f6fdb-qq6dt   2/2     Running   0          45s
```

#### 2.创建存储桶

>客户端可以在其中读取和写入对象。可以通过定义存储类来创建存储桶，类似于块存储和文件存储所使用的模式。首先，定义允许对象客户端创建存储桶的存储类。存储类定义对象存储系统，存储桶保留策略以及管理员所需的其他属性

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f storageclass-bucket-retain.yaml
storageclass.storage.k8s.io/rook-ceph-retain-bucket created
```

#### 3.创建申请声明

>基于此存储类，对象客户端现在可以通过创建对象存储桶声明(OBC)来请求存储桶。创建OBC后，Rook-Ceph存储桶配置程序将创建一个新存储桶。请注意，OBC引用了上面创建的存储类。

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f  object-bucket-claim-retain.yaml
objectbucketclaim.objectbucket.io/ceph-retain-bucket created
```

#### 4.创建集群外部访问

> Rook设置了对象存储，因此Pod可以访问群集内部。如果您的应用程序在集群外部运行，则需要通过来设置外部服务NodePort。
> 首先，请注意将RGW公开到群集内部的服务。我们将保留该服务不变，并为外部访问创建一个新服务。
> 创建之前修改rgw-external.yaml，配置一个固定的nodeport端口例如38000

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f  rgw-external.yaml 
service/rook-ceph-rgw-my-store-external created
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# cat rgw-external.yaml 
apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-rgw-my-store-external
  namespace: rook-ceph # namespace:cluster
  labels:
    app: rook-ceph-rgw
    rook_cluster: rook-ceph # namespace:cluster
    rook_object_store: my-store
spec:
  ports:
    - name: rgw
      port: 80 # service port mentioned in object store crd
      protocol: TCP
      targetPort: 8080
      nodePort: 38000
  selector:
    app: rook-ceph-rgw
    rook_cluster: rook-ceph # namespace:cluster
    rook_object_store: my-store
  sessionAffinity: None
  type: NodePort
```

#### 5.创建一个用户

>如果您需要创建一组独立的用户凭据来访问S3端点，请创建一个CephObjectStoreUser。该用户将使用S3 API连接到集群中的RGW服务。用户将独立于您可能在本文档前面的说明中创建的任何对象存储桶声明。

```bash
root@k8s-master-01:/kubeconfig/rook-1.17.7/deploy/examples# kubectl apply -f object-user.yaml 
cephobjectstoreuser.ceph.rook.io/my-user created

# 获取凭证
kubectl -n rook-ceph get secret rook-ceph-object-user-my-store-my-user -o yaml | grep AccessKey | awk '{print $2}' | base64 --decode 
kubectl -n rook-ceph get secret rook-ceph-object-user-my-store-my-user -o yaml | grep SecretKey | awk '{print $2}' | base64 --decode
```

## 五、ceph基础运维命令

```bash
kubectl exec -it -n rook-ceph rook-ceph-tools-7b75b967db-2sg8z -- bash
```

>https://md2web.gmbaifa.online/#sort=./15_%E5%AD%98%E5%82%A8&doc=01_Ceph/06_Ceph%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/00_Ceph%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4.md

## 六、卸载指南

### 1、卸载顺序

>必须按照以下顺序执行卸载：
>
>1. 删除所有使用 Ceph 存储的 PVC/PV
>2. 删除 CephCluster 自定义资源
>3. 卸载 Rook Ceph Cluster Helm 发布
>4. 卸载 Rook Operator Helm 发布
>5. 清理残留数据

### 2、详细步骤

```bash
rook-ceph 的删除过程

kubectl delete -f toolbox.yaml
kubectl delete -f cluster.yaml

kubectl edit cephclusters.ceph.rook.io -n rook-ceph rook-ceph 
******删除 finalizers 和 下一行


kubectl delete -f operator.yaml

kubectl delete -f crds.yaml -f common.yaml

删除 /var/lib/rook/ 下相关文件，如果是重建集群，osd数据要保留，则不能删除
rm -rf /var/lib/rook/*


kubectl proxy --port=8081 &
kubectl get pod -A|grep Terminating|awk '{print "kubectl delete -n "$1" pod/"$2" --force"}'|sh
ns=`kubectl get ns|grep Terminating|awk '{print $1}'`
mkdir -p /tmp/terminating
for v in $ns; do
echo $v
kubectl get namespace $v -o json |jq 'del(.spec.finalizers)' > /tmp/tmp.json
curl -k -H "Content-Type: application/json" -X PUT --data-binary @/tmp/tmp.json http://127.0
done
sleep 1
ps -ef |grep "kubectl proxy" |grep -v grep |awk '{print $2}'|xargs kill
```

### 3、详细步骤(helm)

#### 1.删除所有PVC/PV

```bash
kubectl delete pvc --all -n rook-ceph
```

#### 2.删除CephClusterCRD

```bash
kubectl -n rook-ceph delete cephcluster rook-ceph
```

#### 3.卸载 Rook Ceph Cluster

```bash
helm uninstall rook-ceph-cluster -n rook-ceph
```

#### 4.卸载 Rook Ceph Operator

```bash
helm uninstall rook-ceph -n rook-ceph
```

#### 5.清理数据（在所有节点执行）

```bash
rm -rf /var/lib/rook #清空数据

#下述以/dev/sdb磁盘举例
wipefs -a /dev/sdb #删除文件系统签名。
sgdisk --zap-all /dev/sdb #删除分区表。
dd if=/dev/zero of=/dev/sdb bs=1M count=100 #覆盖磁盘头部的元数据。
```

#### 6.删除命名空间和 CRD

```bash
kubectl delete namespace rook-ceph
kubectl delete crd cephclusters.ceph.rook.io
```



